import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
	container: {
		flex: 1,
		margin: 24
	},
	button: {
		alignItems: 'center',
		backgroundColor: '#DDDDDD',
		padding: 12,
		margin: 12
	}
});
